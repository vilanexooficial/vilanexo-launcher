# VilaNexo Launcher

Launcher oficial da VilaNexo para os servidores Cobblemon e RPG.

As versões publicadas são geradas automaticamente pelo GitHub Actions quando uma tag `vX.Y.Z` é criada. O instalador usa GitHub Releases para baixar atualizações automaticamente.
